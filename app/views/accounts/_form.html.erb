<%= simple_form_for @account do |f| %>
	 <%= render 'shared/error_messages', modelo: f.object %>
	<%= f.input :name, label: 'Nome' %>
	<%= f.label :tipo %>
	<%= f.select :tipo,[['Entrada', 'E'], ['Saída', 'S']], :prompt => "Selecione o tipo", label: 'Tipo' %>
	<br><br>
	<%= f.button :submit,'Salvar', :class => 'button' %>
<% end %>