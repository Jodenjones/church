<%= render :partial => 'parceiros/show', :locals => {:parceiro => @member.parceiro }%>

<fieldset>
	<legend>Dados do Membro</legend>
	<p>
	  <strong>Entrada:</strong>
	  <%= @member.entrada %>
	</p>
</fieldset>

<%= link_to 'Edit', edit_member_path(@member) %> |
<%= link_to 'Back', members_path %>
